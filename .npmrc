# Prevent npm from creating a package-lock.json in the monorepo root.
# The only npm project here is frontend/ (which has its own lockfile).
# Without this, running `npm install` from the root to manage concurrently
# would create a second package-lock.json that confuses Turbopack's root detection.
package-lock=false
